// Removed legacy duplicate login page.
// Use `src/pages/LoginSelector.tsx` (route: `/login`) and `src/components/auth/UserLoginForm.tsx` instead.

export default function Login() {
  return null;
}
